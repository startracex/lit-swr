{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "check": "biome check --fix",
    "fmt": "biome format --write",
    "lint": "biome lint",
    "version": "changeset version && pnpm run check"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@changesets/cli": "^2.27.11",
    "typescript": "^5.7.2"
  }
}
